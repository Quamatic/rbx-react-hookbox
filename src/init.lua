--[=[
	@class Hooks

	The hooks that the library provides.
]=]

return {
	useEvent = require(script.useEvent),
	useViewportSize = require(script.useViewportSize),
	useCamera = require(script.useCamera),
	useUncontrolled = require(script.useUncontrolled),
	useQueue = require(script.useQueue),
	useListState = require(script.useListState),
	useForceUpdate = require(script.useForceUpdate),
	useCounter = require(script.useCounter),
	useDebouncedState = require(script.useDebouncedState),
	useDebouncedValue = require(script.useDebouncedValue),
	useAttribute = require(script.useAttribute),
	useDisclosure = require(script.useDisclosure),
	useLogger = require(script.useLogger),
	useMergedRef = require(script.useMergedRef),
	useMountedState = require(script.useMountedState),
	useMouse = require(script.useMouse),
	usePrevious = require(script.usePrevious),
	useRendersCount = require(script.useRendersCount),
	useSetState = require(script.useSetState),
	useToggle = require(script.useToggle),
	useUpdateEffect = require(script.useUpdateEffect),
	useValidatedState = require(script.useValidatedState),
	useMove = require(script.useMove),
	useShallowEffect = require(script.useShallowEffect),
	useTimeout = require(script.useTimeout),
	useAsync = require(script.useAsync),
	useAsyncFn = require(script.useAsyncFn),
	useClickOutside = require(script.useClickOutside),
	useInterval = require(script.useInterval),
	useIdle = require(script.useIdle),
	useId = require(script.useId),
	usePagination = require(script.usePagination),
}
